{
  "name": "database",
  "version": "1.0.0",
  "packageManager": "yarn@3.2.2",
  "scripts": {
    "build": "rm -rf dist & tsc",
    "db:update": "ts-node index.ts",
    "clean:database": "rm -rf dist"
  },
  "dependencies": {
    "models": "1.0.0",
    "typescript": "^4.7.4"
  }
}
